# AGENTS.md

When changing anything related to the .config files, always make sure they are synced with the ~/.dotfiles repo.
When you change something, make a commit and push in the ~/.dotfiles repo.
Cherry-pick it to the "work" branch as well, if it contains general config changes that are relevant to my work machine as well. Its windows + wsl (i only work in linux (wsl) there with all the stuff i use here, but the arch/hyprland stuff is obviously useless).
