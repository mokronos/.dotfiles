# See https://git-scm.com/docs/git-config

[alias]
	co = checkout
	br = branch
	ci = commit
	st = status
[init]
	defaultBranch = main
[pull]
	rebase = true 			 # Rebase (instead of merge) on pull
[push]
	autoSetupRemote = true   # Automatically set upstream branch on push
[diff]
	algorithm = histogram    # Clearer diffs on moved/edited lines
	colorMoved = plain       # Highlight moved blocks in diffs
	mnemonicPrefix = true    # More intuitive refs in diff output
[commit]
	verbose = true           # Include diff comment in commit message template
[column]
	ui = auto 			     # Output in columns when possible
[branch]
	sort = -committerdate    # Sort branches by most recent commit first
[tag]
	sort = -version:refname  # Sort version numbers as you would expect
[rerere]
	enabled = true           # Record and reuse conflict resolutions
	autoupdate = true        # Apply stored conflict resolutions automatically
[user]
	name = Sebastian Hirt
	email = sebastian@hirts.de

# Pick the commit identity from the remote a repository actually points at, so
# work never lands under the personal address or the other way round. Each
# includeIf takes one URL form; git has no way to OR them into one condition.
# The scp-style forms need both `:**` and `:*/**`: `**` only crosses `/` when it
# is a whole path component, so `:**` alone stops at the first slash.
# A repository with no matching remote keeps the [user] email above.
[includeIf "hasconfig:remote.*.url:git@github.com:**"]
	path = ~/.config/git/config-github
[includeIf "hasconfig:remote.*.url:git@github.com:*/**"]
	path = ~/.config/git/config-github
[includeIf "hasconfig:remote.*.url:ssh://git@github.com/**"]
	path = ~/.config/git/config-github
[includeIf "hasconfig:remote.*.url:https://github.com/**"]
	path = ~/.config/git/config-github
[includeIf "hasconfig:remote.*.url:git@gitlab.ancud.de:**"]
	path = ~/.config/git/config-gitlab
[includeIf "hasconfig:remote.*.url:git@gitlab.ancud.de:*/**"]
	path = ~/.config/git/config-gitlab
[includeIf "hasconfig:remote.*.url:ssh://git@gitlab.ancud.de:*/**"]
	path = ~/.config/git/config-gitlab
[includeIf "hasconfig:remote.*.url:ssh://git@gitlab.ancud.de/**"]
	path = ~/.config/git/config-gitlab
[includeIf "hasconfig:remote.*.url:https://gitlab.ancud.de/**"]
	path = ~/.config/git/config-gitlab
